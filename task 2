#include<iostream.h>
#include<stdio.h>
#include<conio.h>

void main()
{
   clrscr();
   int arr[5];
   cout << "Enter elements: ";

   for(int i = 0; i < 5; i++)
      cin >> arr[i];

   cout << "You entered: ";
   for(int i = 0; i < 5; i++)
      cout << endl << *(arr + i);

   getch();
}
